public class Calculation {
    int x, y;
    Calculation(int x, int y) {
        this.x = x;
        this.y = y;
    }
    public static void calculationSum(int x, int y) {
        int result = x+y;
        System.out.println("sum of those 2 is " + result);
    }
}
